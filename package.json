{
  "name": "drupalcontractors",
  "version": "1.0.0",
  "description": "Drupal staff augmentation",
  "author": "Albert Volkman",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=master --cleanup"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.9.7",
    "bootstrap": "^4.1.3",
    "bootstrap-vue": "^2.0.0",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
